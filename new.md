Generate a Next.js blog post page or component. The blog post should have the following structure:

1. **Metadata & SEO**
   - Include a `<head>` section with:
     - Title
     - Description
     - Keywords
     - Open Graph (OG) meta tags
     - Twitter Card meta tags
   - Use Next.js `Head` component from 'next/head'

2. **Blog Header**
   - Display blog post title prominently
   - Author name and publication date
   - Optional featured image at the top

3. **Content Section**
   - Well-structured with headings (`h2`, `h3`) and paragraphs
   - Support images and code snippets
   - Include a "Quote" style block component for highlighted text
   - Optional bullet/numbered lists
   - Optional embedded videos or iframes

4. **Sidebar (optional)**
   - Suggested for related posts, tags, or categories

5. **Footer**
   - Author bio section
   - Social sharing buttons (Twitter, LinkedIn, WhatsApp, etc.)
   - Comment section placeholder (if needed)

6. **Styling**
   - Use TailwindCSS or CSS modules
   - Mobile-first responsive layout
   - Clean typography for headings and paragraphs

7. **Reusability**
   - Export as a functional React component
   - Accept props for title, author, date, content, featured image, tags
   - Optional: Markdown support for content

8. **Extra**
   - Include structured data (JSON-LD) for SEO
   - Use semantic HTML (`article`, `section`, `header`, `footer`, `aside`)  

Generate the complete JSX/React code for the blog post page with all the above features.
